# ひとまず 仕様書

## 概要

「ひとまず」は、個人PC 1台で使うことを前提にしたタスク兼メモ管理ツール。

Google ToDoのような軽さを保ちつつ、1件のタスクに長めのメモやサブタスクを持たせられるようにする。Notionほど重くせず、毎日開いても疲れない、温かみと整理しやすさのある3カラムUIを目指す。

## 前提

- GitHub Pagesで公開できる静的Webアプリとして作る。
- サーバー、DB、ログイン機能は持たない。
- データはブラウザの `localStorage` に保存する。
- 別PC、別ブラウザ、スマホとの自動同期はしない。
- データ消失対策としてJSONエクスポート/インポートを持つ。
- 主用途はPCブラウザ。スマホ最適化はMVP対象外。

## コンセプト

ひとまず入れて、あとで整える。

思いついたタスクやメモを素早く入れられることを重視する。入力時に分類や期限を厳密に決めなくてもよく、あとから右カラムの詳細で整理できる。

## デザイン方針

### 目指す雰囲気

- 温かい
- 上品
- おしゃれ
- 文具や手帳のような落ち着き
- 毎日開いても疲れない
- 機械的すぎない
- AIツールっぽさを前面に出さない

### 避ける表現

- AIっぽい紫グラデーション
- 強い影やガラス風の装飾
- 真っ黒なダッシュボード風UI
- SaaS管理画面のような冷たさ
- ボタンや説明文が多すぎる画面

### 色

- 背景: `#F6F3EE`
- 左サイド: `#EFEAE1`
- パネル: `#FFFCF7`
- 文字: `#26231F`
- 補助文字: `#81786D`
- 線: `#DED7CC`
- アクセント: `#2F6F73`
- 大事: `#C66A3D`
- 期限切れ: `#B84A42`
- 完了: `#AAA39A`

## 画面構成

3カラム構成。

```text
左カラム: 時期 + 自作リスト
中央カラム: タスク一覧
右カラム: 選択中タスクの詳細編集
```

カラム境界はドラッグで調整できる。左カラムと右カラムの幅を変えられ、調整値は `localStorage` の別キーに保存する。

レイアウト保存キー:

```text
hitomazu-task-note-layout-v1
```

タスクデータ保存キーとは分ける。

### 左カラム

時期と自作リストを分けて表示する。

```text
時期
- 今日
- 今週
- 今月
- いつか
- 暇なとき
- 習慣
- 未整理

リスト
- ユーザーが自由に追加したテーマ別リスト
```

「時期」はいつやるかを表す。「リスト」はテーマ別のまとまりを表す。

### 中央カラム

選択中の時期またはリストに属するタスクを表示する。

表示は少しゆったりめにする。

表示する情報:

- タイトル
- メモ抜粋
- サブタスク進捗
- 優先度
- 時期
- リスト
- 期限

### 右カラム

選択中タスクをその場で編集する。

現在の時期/リスト/検索条件に含まれないタスクは、右カラムに残さない。左カラムや検索条件を切り替えたとき、表示中一覧の先頭タスクを選択し直す。該当タスクがなければ右カラムは空状態にする。

理由:

- 最後に触ったタスクが右側に残ると、現在見ている一覧のタスクだと誤認しやすい。
- 中央一覧と右詳細の対応関係を明確にする。

編集項目:

- タイトル
- メモ
- サブタスク
- 時期
- リスト
- 優先度
- 期限
- 作成日
- 更新日
- 削除

## データモデル

### Task

```json
{
  "id": "task_xxx",
  "title": "Claude環境作成",
  "period": "week",
  "listId": "list_claude",
  "priority": "important",
  "done": false,
  "memo": "参考URLや作業メモ",
  "subtasks": [
    {
      "id": "sub_xxx",
      "title": "フォルダ構成を学ぶ",
      "done": false
    }
  ],
  "dueDate": "2026-08-01",
  "createdAt": "2026-07-30T10:00:00.000Z",
  "updatedAt": "2026-07-30T10:00:00.000Z",
  "completedAt": null
}
```

### List

```json
{
  "id": "list_xxx",
  "name": "Claude環境作成"
}
```

### period

```text
today: 今日
week: 今週
month: 今月
someday: いつか
free: 暇なとき
habit: 習慣
unsorted: 未整理
```

### priority

```text
important: 大事
normal: ふつう
low: 低め
```

## 重要な設計判断

### 保存済みデータの扱い

既存ユーザーのタスクデータを壊さないため、タスク保存キー `hitomazu-task-note-v1` と既存データ構造は維持する。

UIレイアウトの幅調整は新しい保存キー `hitomazu-task-note-layout-v1` に保存する。これにより、既存タスクデータの移行は不要。

### タスクの基本単位

短いタスクと、メモ付きの大きめタスクを同じデータ型で扱う。

理由:

- 画像例では、1行タスクよりも作業メモに近いまとまりが多い。
- 「Claude環境作成」のような大きめの作業にメモや小さな手順を持たせたい。
- タスクとメモを分けすぎると入力が重くなる。

### 時期とリストを分ける

タスクは `period` と `listId` を別々に持つ。

例:

```text
タイトル: Claude環境作成
時期: 今週
リスト: Claude環境作成
優先度: 大事
```

理由:

- 「いつやるか」と「何のテーマか」は別の概念。
- 同じタスクを「今週」からも「Claude環境作成」からも見られる。
- 優先度だけでは中期/長期の整理はできない。

### Inboxではなく未整理

日本語UIとして `Inbox` は分かりづらいため、固定時期として `未整理` を使う。

役割:

- とりあえず入れたもの
- あとで時期やリストを整えるもの

### 習慣

MVPでは繰り返し機能を持たない。

`習慣` は通常の時期の1つとして扱う。毎日/週数回などの繰り返しチェックは将来拡張とする。

理由:

- 繰り返し機能は仕様が複雑になりやすい。
- 最初は「習慣として置いておける場所」があれば十分。

## 入力仕様

### クイック追加

上部入力欄に入力してEnterで追加する。

追加先:

- 時期を見ている場合、その時期に追加する。
- 自作リストを見ている場合、そのリストに追加し、時期は `未整理` にする。

理由:

- 今見ている場所に追加される方が直感的。
- 入力時の分類負担を減らせる。

### 詳細編集

右カラムで即時編集する。

保存ボタンは置かない。すべて操作ごとに自動保存する。

## ソート仕様

ソートは保存時ではなく、表示を描き直すたびに行う。

対象:

- 未完了タスクのみ自動ソートする。
- 完了済みタスクは通常の未完了一覧には混ぜない。

タイミング:

- タスク追加
- 優先度変更
- 期限変更
- 完了切り替え
- 時期変更
- リスト変更
- 検索
- 左カラムで表示先を変更
- ページ再読み込み

並び順:

```text
1. 期限切れ
2. 今日期限
3. 明日期限
4. 優先度: 大事
5. 優先度: ふつう
6. 優先度: 低め
7. 期限なし
8. 更新が新しい順
```

実装上は、期限ランク、優先度ランク、更新日時の順で比較する。

## 完了タスク仕様

完了済みタスクは、今日中だけ一覧下部に薄く表示する。

昨日以前に完了したタスクは、通常表示から隠す。

理由:

- 完了直後の達成感や誤操作の戻しやすさを残す。
- 古い完了タスクで日常画面が散らからない。

将来的には「完了ログ」画面を追加できる。

## 検索仕様

検索はMVPから入れる。

検索対象:

- タイトル
- メモ
- サブタスク
- リスト名

ショートカット:

- `/`: 検索欄へ移動

## バックアップ仕様

右上メニューから操作する。

メニュー項目:

- データを書き出す
- データを読み込む
- このツールについて

### 書き出し

現在の `state` をJSONファイルとしてダウンロードする。

ファイル名:

```text
hitomazu-backup-YYYY-MM-DD.json
```

### 読み込み

JSONを読み込み、現在のデータを置き換える。

置き換え前に確認ダイアログを出す。

## ヘルプ仕様

右上メニューの「このツールについて」から控えめなダイアログを開く。

通常画面には説明文を多く置かず、困ったときだけ確認できるようにする。

含める内容:

- 保存について
- 表示について
- リストについて
- バックアップについて

## 削除仕様

MVPでは確認ありの完全削除。

ゴミ箱は作らない。

将来的に誤削除が気になる場合は、ゴミ箱またはUndoを追加する。

## キーボード操作

```text
Enter: クイック追加、サブタスク追加
Esc: メニュー/ヘルプを閉じる
/: 検索欄へ移動
```

## ファイル構成

```text
index.html
style.css
app.js
specification.md
.github/workflows/pages.yml
```

GitHub Pagesでは、`index.html`、`style.css`、`app.js` を同じディレクトリに配置する。

## MVP外

- ログイン
- クラウド同期
- Firebase/Supabase連携
- スマホ向け本格UI
- 繰り返し習慣
- ゴミ箱
- 手動並び替え
- タグ
- 複数端末共有
- GitHub APIによるJSON保存

## 将来拡張候補

- 習慣の繰り返し設定
- 完了ログ
- ゴミ箱またはUndo
- リストごとの自動/手動ソート切り替え
- タグ
- JSONバックアップの定期リマインド
- Markdown風メモ入力
- PWA化
- Firebase/Supabaseによる同期
